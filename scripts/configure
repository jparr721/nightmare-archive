#!/bin/bash

if [ ! -d "build" ]; then
  mkdir build
fi
cd build
cmake -GNinja ..
cd ..

if [ -f "compile_commands.json" ]; then
  rm compile_commands.json
fi
ln -s $(pwd)/build/compile_commands.json $(pwd)
